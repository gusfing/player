// Minimal layout for video player pages
// Renders as standalone HTML without Next.js default styles

export default function PlayerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
